<div class="box-table">
    <div class="table">
        <% head = {
            turma: "Turma",
            "count(*)": "Quant. de registros",
            visualizacao: "Visualização"
        } %>

        <%- include('./tabela', {
            head: head,

            tabela: tabela["data"]
        }) %>
    </div>
</div>